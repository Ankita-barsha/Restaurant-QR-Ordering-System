# Copy to .env:  cp .env.example .env
# This file IS committed. .env is NOT.
#
# WARNING: every VITE_ variable is embedded in the JS bundle and is publicly
# readable. Never put secrets, API keys, or tokens here.

# Base URL of the backend API (server/.env PORT)
VITE_API_URL="http://localhost:5000"
